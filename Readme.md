search the playwirght documention to know what to do 
install playwrught
pip nstall playwright

playwright & playwright with pytest

pytest ?
pytest can be used with any of python framwork

if you already installed the playwright , pip install pytest-playwright

there will be noconflict
even when you already installed the playwright.

pyetst-?fixture and cli support.